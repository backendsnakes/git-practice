# git-practice

## Usage

```bash
$ git clone
$ git checkout -b feature/branch
$ git add .
$ git commit -m "Add: README.md"
$ git push origin feature/branch
```

## Install

```bash
$ git clone
$ cd git-practice
$ poetry install
```